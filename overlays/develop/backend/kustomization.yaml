apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/backend
  - real-plain-secret.yaml

patches:
  - path: patch-image.yaml
  - path: patch-env.yaml

generatorOptions:
  disableNameSuffixHash: true
